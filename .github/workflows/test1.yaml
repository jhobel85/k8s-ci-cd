name: My Github action workflow

on:
  push:
    branches:
      - main
  commit:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest
    container:
      image: betterdev/k8s-ci-cd:latest # <-- Use this docker image
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run test step
        run: echo "Running v1.0.0 of this awesome 🐳 Docker image"